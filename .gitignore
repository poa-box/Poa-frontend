# Cloned reference repos (not tracked)
POP/
subgraph-pop/

# Playwright MCP artifacts (snapshots, console logs, recordings)
.playwright-mcp/
/*.gif
/poa-app/*.gif

# Claude Code session state (per-workspace, not for source control)
.claude/
