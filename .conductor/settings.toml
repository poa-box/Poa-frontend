"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "bash .conductor/setup.sh"
archive = "rm -rf poa-app/node_modules poa-app/.next .smithers/node_modules node_modules .next"
run_mode = "concurrent"
