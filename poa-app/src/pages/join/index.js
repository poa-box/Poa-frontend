import Page from '@/components/join/JoinPage';

// Keep metadata available above the application providers during initial load.
Page.seo = {
    "title": "Join Organization",
    "description": "Join a community-owned organization.",
    "path": "/join",
    "noIndex": true
  };

export default Page;
