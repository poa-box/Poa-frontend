import Page from '@/features/application/pages/LeaderboardPage';

// Keep metadata available above the application providers during initial load.
Page.seo = {
    "title": "Leaderboard",
    "description": "Organization contribution leaderboard.",
    "path": "/leaderboard",
    "noIndex": true
  };

export default Page;
