import Page from '@/features/application/pages/TeamPage';

// Keep metadata available above the application providers during initial load.
Page.seo = {
    "title": "Organization Structure",
    "description": "View organization roles and governance structure.",
    "path": "/team",
    "noIndex": true
  };

export default Page;
