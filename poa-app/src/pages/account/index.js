import Page from '@/features/application/pages/AccountPage';

// Keep metadata available above the application providers during initial load.
Page.seo = {
    "title": "Account Settings",
    "description": "Manage your Poa account settings and profile.",
    "path": "/account",
    "noIndex": true
  };

export default Page;
